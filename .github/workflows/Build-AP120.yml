name: Build OpenWrt Image for AP120

on:
  schedule:
    - cron: '0 0 * * Sat'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/GenericBuildWorkflow.yml
    with:
      workflow: Development
      version: main
      device: AP120
      rebase: true
