name: Build OpenWrt Development Image
run-name: Build ${{ inputs.repository }}, brach ${{ inputs.branch }}

on:
  workflow_dispatch:
    inputs:
      repository:
        description: "OpenWrt Repository URL (optional)"
        required: false
        default: "RolandoMagico/openwrt"
      branch:
        description: "Branch to build"
        required: true
        type: choice
        options:
          - E30
          - MR9000
      ci_directory:
        description: "The directory where the CI repository will be checked out to"
        required: false
        default: "CI"
      openwrt_directory:
        description: "The directory where the OpenWrt repository will be checked out to"
        required: false
        default: "openwrt"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Workflow Repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.ref }}
          path: ${{ inputs.ci_directory }}

      - name: Checkout OpenWrt Repository
        uses: actions/checkout@v6
        with:
          repository: ${{ inputs.repository }}
          ref: ${{ inputs.branch }}
          path: ${{ inputs.openwrt_directory }}

      - name: Install Dependencies
        run: |
          sudo apt update
          sudo apt install build-essential clang flex bison g++ gawk \
          gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
          python3-setuptools rsync swig unzip zlib1g-dev file wget

      - name: Update Feeds
        run: cd ${{ inputs.openwrt_directory }} && ./scripts/feeds update -a

      - name: Install Feeds
        run: cd ${{ inputs.openwrt_directory }} && ./scripts/feeds install -a

      - name: Copy basic config
        run: cp ${{ inputs.ci_directory }}/${{ inputs.branch }}/diffconfig ${{ inputs.openwrt_directory }}/.config

      - name: Make default config
        run: cd ${{ inputs.openwrt_directory }} && make defconfig

      - name: Build Firmware
        run: |
          cd ${{ inputs.openwrt_directory }}
          make -j$(nproc) || make -j1 V=s

      - name: Upload Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: openwrt-${{ inputs.branch }}
          path: ${{ inputs.openwrt_directory }}/bin/
